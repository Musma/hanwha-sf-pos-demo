# Design token evidence

통합 화면은 저장소 루트의 `DESIGN.md`를 기준으로 기존 SF-POS 색상, 타이포그래피, 간격, 경계 체계를 보존한다.

- 새 시각 요소는 클릭 가능한 메뉴의 pointer cursor와 `#0a72f2` focus ring뿐이다.
- 워크프론트 화면의 `#6e5c4c`, `#c0261d` 버튼 색상을 기존 값 그대로 사용한다.
- 새 애니메이션, 그림자, 그라디언트, 레이아웃 변형을 추가하지 않았다.

